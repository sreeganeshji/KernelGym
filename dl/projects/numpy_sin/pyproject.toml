[project]
name = "numpy-sin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ganesh Bangalore" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
numpy-sin = "numpy_sin:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
